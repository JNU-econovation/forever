spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
  flyway:
    enabled: false
  mail:
    host: localhost
    port: 3025
    username: test
    password: test

# JWT 설정
jwt:
  secret: test-secret-key-for-testing-purposes-only-this-should-be-very-long-and-secure-in-production-environment
  token-validity-in-seconds: 86400
